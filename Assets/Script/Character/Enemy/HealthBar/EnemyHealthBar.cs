using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class EnemyHealthBar : MonoBehaviour
{
/*
    EnemyStats stats;
    Camera isoCamera;

    public Image healthBar;

    void Start()
    {
		stats = GetComponent<EnemyStats>();
		healthBar = GetComponent<Image>();
        isoCamera = GameObject.FindGameObjectWithTag("MainCamera").GetComponent<Camera>() as Camera;
    }

    // Update is called once per frame
    void Update()
    {
        LookAtCamera();
        UpdateHealthBar();
    }


    #region healthBar
    void LookAtCamera(){
        this.transform.LookAt(this.transform.position + isoCamera.transform.rotation * Vector3.back, isoCamera.transform.rotation * Vector3.up);
    }
    void UpdateHealthBar()
    {
        healthBar.fillAmount = stats

        if (healthBar.fillAmount < 0.3f)
        {
            healthBar.color = Color.red;
        }
        else if(healthBar.fillAmount < 0.6f)
        {
            healthBar.color = Color.yellow;
        }
    }
    #endregion*/
}
